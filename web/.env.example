# Copy to ".env". These VITE_* values are bundled into the web app and are NOT
# secrets (Stadia key + Supabase publishable key are meant to ship in clients).
# Never put a Supabase SECRET key here.

VITE_STADIA_API_KEY=
VITE_MAP_STYLE=

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
